#!/usr/bin/env bash
set -euo pipefail

usage() {
    echo "Usage: git fetch-branch REMOTE BRANCH" >&2
    exit 2
}

if [ $# -ne 2 ]; then
    usage
fi

remote=$1
branch=$2

exec git fetch "$remote" "+refs/heads/$branch:refs/remotes/$remote/$branch"
